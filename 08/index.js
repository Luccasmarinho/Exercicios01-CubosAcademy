const PI = 3.14;
const raio = 2;
const comprimento = 2 * PI * raio;
const area = PI * raio ** 2;

console.log(`O comprimento do círculo é de: ${comprimento}`);
console.log(`A área do círculo é de: ${area}`);